{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
      "dev": { "cache": false, "persistent": true },
      "build": {
        "dependsOn": ["^build"],
        "outputs": ["dist/**"],
        "passThroughEnv": ["DATABASE_URL"]
      },
      "lint": {},
      "typecheck": { "dependsOn": ["^build"], "passThroughEnv": ["DATABASE_URL"] },
      "test": {}
    }
  }